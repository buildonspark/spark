---
"@buildonspark/spark-sdk": minor
---

- Require Node.js signing-operator TLS verification by default, trust the local minikube CA automatically for hermetic tests, and keep certificate verification bypass local-only.
