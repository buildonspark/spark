# Which operators/SSP to target: "default" (released SDK regtest defaults) or "loadtest"
BREEZ_CONFORMANCE_STACK=default

# Optional: log level for the suite (default INFO)
# BREEZ_CONFORMANCE_LOG_LEVEL=INFO
