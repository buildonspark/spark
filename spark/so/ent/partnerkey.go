// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/lightsparkdev/spark/common/keys/jwt"
	"github.com/lightsparkdev/spark/so/ent/partnerkey"
)

// PartnerKey is the model entity for the PartnerKey schema.
type PartnerKey struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// The time when the entity was created.
	CreateTime time.Time `json:"create_time,omitempty"`
	// The time when the entity was last updated.
	UpdateTime time.Time `json:"update_time,omitempty"`
	// Identifier for the partner, included as the 'iss' claim in their JWT.
	PartnerID string `json:"partner_id,omitempty"`
	// Human-readable display name for the partner.
	PartnerName string `json:"partner_name,omitempty"`
	// Compressed public key (34 bytes: 1-byte curve discriminator + 33-byte compressed key) used to verify partner JWTs. Supports both secp256k1 (ES256K) and P-256 (ES256).
	JwtPublicKey jwt.Public `json:"jwt_public_key,omitempty"`
	// Argon2id hash (PHC-encoded) of the shared secret used to authenticate the partner via HTTP Basic Auth (base64(partner_id:secret)). The raw secret is never stored; the auth interceptor verifies the presented secret against this hash with a constant-time compare. Nullable: only set for partners that use the Basic Auth flow.
	BasicAuthSecretHash string `json:"-"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PartnerKeyQuery when eager-loading is set.
	Edges        PartnerKeyEdges `json:"edges"`
	selectValues sql.SelectValues
}

// PartnerKeyEdges holds the relations/edges for other nodes in the graph.
type PartnerKeyEdges struct {
	// All label-specific partner entries associated with this key.
	Partners []*Partner `json:"partners,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// PartnersOrErr returns the Partners value or an error if the edge
// was not loaded in eager-loading.
func (e PartnerKeyEdges) PartnersOrErr() ([]*Partner, error) {
	if e.loadedTypes[0] {
		return e.Partners, nil
	}
	return nil, &NotLoadedError{edge: "partners"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*PartnerKey) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case partnerkey.FieldJwtPublicKey:
			values[i] = new(jwt.Public)
		case partnerkey.FieldPartnerID, partnerkey.FieldPartnerName, partnerkey.FieldBasicAuthSecretHash:
			values[i] = new(sql.NullString)
		case partnerkey.FieldCreateTime, partnerkey.FieldUpdateTime:
			values[i] = new(sql.NullTime)
		case partnerkey.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the PartnerKey fields.
func (pk *PartnerKey) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case partnerkey.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				pk.ID = *value
			}
		case partnerkey.FieldCreateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field create_time", values[i])
			} else if value.Valid {
				pk.CreateTime = value.Time
			}
		case partnerkey.FieldUpdateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field update_time", values[i])
			} else if value.Valid {
				pk.UpdateTime = value.Time
			}
		case partnerkey.FieldPartnerID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field partner_id", values[i])
			} else if value.Valid {
				pk.PartnerID = value.String
			}
		case partnerkey.FieldPartnerName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field partner_name", values[i])
			} else if value.Valid {
				pk.PartnerName = value.String
			}
		case partnerkey.FieldJwtPublicKey:
			if value, ok := values[i].(*jwt.Public); !ok {
				return fmt.Errorf("unexpected type %T for field jwt_public_key", values[i])
			} else if value != nil {
				pk.JwtPublicKey = *value
			}
		case partnerkey.FieldBasicAuthSecretHash:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field basic_auth_secret_hash", values[i])
			} else if value.Valid {
				pk.BasicAuthSecretHash = value.String
			}
		default:
			pk.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the PartnerKey.
// This includes values selected through modifiers, order, etc.
func (pk *PartnerKey) Value(name string) (ent.Value, error) {
	return pk.selectValues.Get(name)
}

// QueryPartners queries the "partners" edge of the PartnerKey entity.
func (pk *PartnerKey) QueryPartners() *PartnerQuery {
	return NewPartnerKeyClient(pk.config).QueryPartners(pk)
}

// Update returns a builder for updating this PartnerKey.
// Note that you need to call PartnerKey.Unwrap() before calling this method if this PartnerKey
// was returned from a transaction, and the transaction was committed or rolled back.
func (pk *PartnerKey) Update() *PartnerKeyUpdateOne {
	return NewPartnerKeyClient(pk.config).UpdateOne(pk)
}

// Unwrap unwraps the PartnerKey entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (pk *PartnerKey) Unwrap() *PartnerKey {
	_tx, ok := pk.config.driver.(*txDriver)
	if !ok {
		panic("ent: PartnerKey is not a transactional entity")
	}
	pk.config.driver = _tx.drv
	return pk
}

// String implements the fmt.Stringer.
func (pk *PartnerKey) String() string {
	var builder strings.Builder
	builder.WriteString("PartnerKey(")
	builder.WriteString(fmt.Sprintf("id=%v, ", pk.ID))
	builder.WriteString("create_time=")
	builder.WriteString(pk.CreateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("update_time=")
	builder.WriteString(pk.UpdateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("partner_id=")
	builder.WriteString(pk.PartnerID)
	builder.WriteString(", ")
	builder.WriteString("partner_name=")
	builder.WriteString(pk.PartnerName)
	builder.WriteString(", ")
	builder.WriteString("jwt_public_key=")
	builder.WriteString(fmt.Sprintf("%v", pk.JwtPublicKey))
	builder.WriteString(", ")
	builder.WriteString("basic_auth_secret_hash=<sensitive>")
	builder.WriteByte(')')
	return builder.String()
}

// PartnerKeys is a parsable slice of PartnerKey.
type PartnerKeys []*PartnerKey
