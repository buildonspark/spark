// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"time"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/lightsparkdev/spark/common/keys"
	"github.com/lightsparkdev/spark/so/ent/delegationgrant"
	"github.com/lightsparkdev/spark/so/ent/leafdecomposition"
	"github.com/lightsparkdev/spark/so/ent/schema/schematype"
	"github.com/lightsparkdev/spark/so/ent/signingkeyshare"
	"github.com/lightsparkdev/spark/so/ent/treenode"
)

// LeafDecompositionCreate is the builder for creating a LeafDecomposition entity.
type LeafDecompositionCreate struct {
	config
	mutation *LeafDecompositionMutation
	hooks    []Hook
	conflict []sql.ConflictOption
}

// SetCreateTime sets the "create_time" field.
func (ldc *LeafDecompositionCreate) SetCreateTime(t time.Time) *LeafDecompositionCreate {
	ldc.mutation.SetCreateTime(t)
	return ldc
}

// SetNillableCreateTime sets the "create_time" field if the given value is not nil.
func (ldc *LeafDecompositionCreate) SetNillableCreateTime(t *time.Time) *LeafDecompositionCreate {
	if t != nil {
		ldc.SetCreateTime(*t)
	}
	return ldc
}

// SetUpdateTime sets the "update_time" field.
func (ldc *LeafDecompositionCreate) SetUpdateTime(t time.Time) *LeafDecompositionCreate {
	ldc.mutation.SetUpdateTime(t)
	return ldc
}

// SetNillableUpdateTime sets the "update_time" field if the given value is not nil.
func (ldc *LeafDecompositionCreate) SetNillableUpdateTime(t *time.Time) *LeafDecompositionCreate {
	if t != nil {
		ldc.SetUpdateTime(*t)
	}
	return ldc
}

// SetDelegateSigningPubkey sets the "delegate_signing_pubkey" field.
func (ldc *LeafDecompositionCreate) SetDelegateSigningPubkey(k keys.Public) *LeafDecompositionCreate {
	ldc.mutation.SetDelegateSigningPubkey(k)
	return ldc
}

// SetStatus sets the "status" field.
func (ldc *LeafDecompositionCreate) SetStatus(sds schematype.LeafDecompositionStatus) *LeafDecompositionCreate {
	ldc.mutation.SetStatus(sds)
	return ldc
}

// SetNillableStatus sets the "status" field if the given value is not nil.
func (ldc *LeafDecompositionCreate) SetNillableStatus(sds *schematype.LeafDecompositionStatus) *LeafDecompositionCreate {
	if sds != nil {
		ldc.SetStatus(*sds)
	}
	return ldc
}

// SetID sets the "id" field.
func (ldc *LeafDecompositionCreate) SetID(u uuid.UUID) *LeafDecompositionCreate {
	ldc.mutation.SetID(u)
	return ldc
}

// SetNillableID sets the "id" field if the given value is not nil.
func (ldc *LeafDecompositionCreate) SetNillableID(u *uuid.UUID) *LeafDecompositionCreate {
	if u != nil {
		ldc.SetID(*u)
	}
	return ldc
}

// SetTreeNodeID sets the "tree_node" edge to the TreeNode entity by ID.
func (ldc *LeafDecompositionCreate) SetTreeNodeID(id uuid.UUID) *LeafDecompositionCreate {
	ldc.mutation.SetTreeNodeID(id)
	return ldc
}

// SetTreeNode sets the "tree_node" edge to the TreeNode entity.
func (ldc *LeafDecompositionCreate) SetTreeNode(t *TreeNode) *LeafDecompositionCreate {
	return ldc.SetTreeNodeID(t.ID)
}

// SetSigningKeyshareID sets the "signing_keyshare" edge to the SigningKeyshare entity by ID.
func (ldc *LeafDecompositionCreate) SetSigningKeyshareID(id uuid.UUID) *LeafDecompositionCreate {
	ldc.mutation.SetSigningKeyshareID(id)
	return ldc
}

// SetNillableSigningKeyshareID sets the "signing_keyshare" edge to the SigningKeyshare entity by ID if the given value is not nil.
func (ldc *LeafDecompositionCreate) SetNillableSigningKeyshareID(id *uuid.UUID) *LeafDecompositionCreate {
	if id != nil {
		ldc = ldc.SetSigningKeyshareID(*id)
	}
	return ldc
}

// SetSigningKeyshare sets the "signing_keyshare" edge to the SigningKeyshare entity.
func (ldc *LeafDecompositionCreate) SetSigningKeyshare(s *SigningKeyshare) *LeafDecompositionCreate {
	return ldc.SetSigningKeyshareID(s.ID)
}

// SetDelegationGrantID sets the "delegation_grant" edge to the DelegationGrant entity by ID.
func (ldc *LeafDecompositionCreate) SetDelegationGrantID(id uuid.UUID) *LeafDecompositionCreate {
	ldc.mutation.SetDelegationGrantID(id)
	return ldc
}

// SetDelegationGrant sets the "delegation_grant" edge to the DelegationGrant entity.
func (ldc *LeafDecompositionCreate) SetDelegationGrant(d *DelegationGrant) *LeafDecompositionCreate {
	return ldc.SetDelegationGrantID(d.ID)
}

// Mutation returns the LeafDecompositionMutation object of the builder.
func (ldc *LeafDecompositionCreate) Mutation() *LeafDecompositionMutation {
	return ldc.mutation
}

// Save creates the LeafDecomposition in the database.
func (ldc *LeafDecompositionCreate) Save(ctx context.Context) (*LeafDecomposition, error) {
	ldc.defaults()
	return withHooks(ctx, ldc.sqlSave, ldc.mutation, ldc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (ldc *LeafDecompositionCreate) SaveX(ctx context.Context) *LeafDecomposition {
	v, err := ldc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (ldc *LeafDecompositionCreate) Exec(ctx context.Context) error {
	_, err := ldc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (ldc *LeafDecompositionCreate) ExecX(ctx context.Context) {
	if err := ldc.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (ldc *LeafDecompositionCreate) defaults() {
	if _, ok := ldc.mutation.CreateTime(); !ok {
		v := leafdecomposition.DefaultCreateTime()
		ldc.mutation.SetCreateTime(v)
	}
	if _, ok := ldc.mutation.UpdateTime(); !ok {
		v := leafdecomposition.DefaultUpdateTime()
		ldc.mutation.SetUpdateTime(v)
	}
	if _, ok := ldc.mutation.Status(); !ok {
		v := leafdecomposition.DefaultStatus
		ldc.mutation.SetStatus(v)
	}
	if _, ok := ldc.mutation.ID(); !ok {
		v := leafdecomposition.DefaultID()
		ldc.mutation.SetID(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (ldc *LeafDecompositionCreate) check() error {
	if _, ok := ldc.mutation.CreateTime(); !ok {
		return &ValidationError{Name: "create_time", err: errors.New(`ent: missing required field "LeafDecomposition.create_time"`)}
	}
	if _, ok := ldc.mutation.UpdateTime(); !ok {
		return &ValidationError{Name: "update_time", err: errors.New(`ent: missing required field "LeafDecomposition.update_time"`)}
	}
	if _, ok := ldc.mutation.DelegateSigningPubkey(); !ok {
		return &ValidationError{Name: "delegate_signing_pubkey", err: errors.New(`ent: missing required field "LeafDecomposition.delegate_signing_pubkey"`)}
	}
	if _, ok := ldc.mutation.Status(); !ok {
		return &ValidationError{Name: "status", err: errors.New(`ent: missing required field "LeafDecomposition.status"`)}
	}
	if v, ok := ldc.mutation.Status(); ok {
		if err := leafdecomposition.StatusValidator(v); err != nil {
			return &ValidationError{Name: "status", err: fmt.Errorf(`ent: validator failed for field "LeafDecomposition.status": %w`, err)}
		}
	}
	if len(ldc.mutation.TreeNodeIDs()) == 0 {
		return &ValidationError{Name: "tree_node", err: errors.New(`ent: missing required edge "LeafDecomposition.tree_node"`)}
	}
	if len(ldc.mutation.DelegationGrantIDs()) == 0 {
		return &ValidationError{Name: "delegation_grant", err: errors.New(`ent: missing required edge "LeafDecomposition.delegation_grant"`)}
	}
	return nil
}

func (ldc *LeafDecompositionCreate) sqlSave(ctx context.Context) (*LeafDecomposition, error) {
	if err := ldc.check(); err != nil {
		return nil, err
	}
	_node, _spec := ldc.createSpec()
	if err := sqlgraph.CreateNode(ctx, ldc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	if _spec.ID.Value != nil {
		if id, ok := _spec.ID.Value.(*uuid.UUID); ok {
			_node.ID = *id
		} else if err := _node.ID.Scan(_spec.ID.Value); err != nil {
			return nil, err
		}
	}
	ldc.mutation.id = &_node.ID
	ldc.mutation.done = true
	return _node, nil
}

func (ldc *LeafDecompositionCreate) createSpec() (*LeafDecomposition, *sqlgraph.CreateSpec) {
	var (
		_node = &LeafDecomposition{config: ldc.config}
		_spec = sqlgraph.NewCreateSpec(leafdecomposition.Table, sqlgraph.NewFieldSpec(leafdecomposition.FieldID, field.TypeUUID))
	)
	_spec.OnConflict = ldc.conflict
	if id, ok := ldc.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = &id
	}
	if value, ok := ldc.mutation.CreateTime(); ok {
		_spec.SetField(leafdecomposition.FieldCreateTime, field.TypeTime, value)
		_node.CreateTime = value
	}
	if value, ok := ldc.mutation.UpdateTime(); ok {
		_spec.SetField(leafdecomposition.FieldUpdateTime, field.TypeTime, value)
		_node.UpdateTime = value
	}
	if value, ok := ldc.mutation.DelegateSigningPubkey(); ok {
		_spec.SetField(leafdecomposition.FieldDelegateSigningPubkey, field.TypeBytes, value)
		_node.DelegateSigningPubkey = value
	}
	if value, ok := ldc.mutation.Status(); ok {
		_spec.SetField(leafdecomposition.FieldStatus, field.TypeEnum, value)
		_node.Status = value
	}
	if nodes := ldc.mutation.TreeNodeIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: false,
			Table:   leafdecomposition.TreeNodeTable,
			Columns: []string{leafdecomposition.TreeNodeColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(treenode.FieldID, field.TypeUUID),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.leaf_decomposition_tree_node = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := ldc.mutation.SigningKeyshareIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: false,
			Table:   leafdecomposition.SigningKeyshareTable,
			Columns: []string{leafdecomposition.SigningKeyshareColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(signingkeyshare.FieldID, field.TypeUUID),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.leaf_decomposition_signing_keyshare = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := ldc.mutation.DelegationGrantIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   leafdecomposition.DelegationGrantTable,
			Columns: []string{leafdecomposition.DelegationGrantColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(delegationgrant.FieldID, field.TypeUUID),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.delegation_grant_leaf_decompositions = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// OnConflict allows configuring the `ON CONFLICT` / `ON DUPLICATE KEY` clause
// of the `INSERT` statement. For example:
//
//	client.LeafDecomposition.Create().
//		SetCreateTime(v).
//		OnConflict(
//			// Update the row with the new values
//			// the was proposed for insertion.
//			sql.ResolveWithNewValues(),
//		).
//		// Override some of the fields with custom
//		// update values.
//		Update(func(u *ent.LeafDecompositionUpsert) {
//			SetCreateTime(v+v).
//		}).
//		Exec(ctx)
func (ldc *LeafDecompositionCreate) OnConflict(opts ...sql.ConflictOption) *LeafDecompositionUpsertOne {
	ldc.conflict = opts
	return &LeafDecompositionUpsertOne{
		create: ldc,
	}
}

// OnConflictColumns calls `OnConflict` and configures the columns
// as conflict target. Using this option is equivalent to using:
//
//	client.LeafDecomposition.Create().
//		OnConflict(sql.ConflictColumns(columns...)).
//		Exec(ctx)
func (ldc *LeafDecompositionCreate) OnConflictColumns(columns ...string) *LeafDecompositionUpsertOne {
	ldc.conflict = append(ldc.conflict, sql.ConflictColumns(columns...))
	return &LeafDecompositionUpsertOne{
		create: ldc,
	}
}

type (
	// LeafDecompositionUpsertOne is the builder for "upsert"-ing
	//  one LeafDecomposition node.
	LeafDecompositionUpsertOne struct {
		create *LeafDecompositionCreate
	}

	// LeafDecompositionUpsert is the "OnConflict" setter.
	LeafDecompositionUpsert struct {
		*sql.UpdateSet
	}
)

// SetUpdateTime sets the "update_time" field.
func (u *LeafDecompositionUpsert) SetUpdateTime(v time.Time) *LeafDecompositionUpsert {
	u.Set(leafdecomposition.FieldUpdateTime, v)
	return u
}

// UpdateUpdateTime sets the "update_time" field to the value that was provided on create.
func (u *LeafDecompositionUpsert) UpdateUpdateTime() *LeafDecompositionUpsert {
	u.SetExcluded(leafdecomposition.FieldUpdateTime)
	return u
}

// SetStatus sets the "status" field.
func (u *LeafDecompositionUpsert) SetStatus(v schematype.LeafDecompositionStatus) *LeafDecompositionUpsert {
	u.Set(leafdecomposition.FieldStatus, v)
	return u
}

// UpdateStatus sets the "status" field to the value that was provided on create.
func (u *LeafDecompositionUpsert) UpdateStatus() *LeafDecompositionUpsert {
	u.SetExcluded(leafdecomposition.FieldStatus)
	return u
}

// UpdateNewValues updates the mutable fields using the new values that were set on create except the ID field.
// Using this option is equivalent to using:
//
//	client.LeafDecomposition.Create().
//		OnConflict(
//			sql.ResolveWithNewValues(),
//			sql.ResolveWith(func(u *sql.UpdateSet) {
//				u.SetIgnore(leafdecomposition.FieldID)
//			}),
//		).
//		Exec(ctx)
func (u *LeafDecompositionUpsertOne) UpdateNewValues() *LeafDecompositionUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithNewValues())
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(s *sql.UpdateSet) {
		if _, exists := u.create.mutation.ID(); exists {
			s.SetIgnore(leafdecomposition.FieldID)
		}
		if _, exists := u.create.mutation.CreateTime(); exists {
			s.SetIgnore(leafdecomposition.FieldCreateTime)
		}
		if _, exists := u.create.mutation.DelegateSigningPubkey(); exists {
			s.SetIgnore(leafdecomposition.FieldDelegateSigningPubkey)
		}
	}))
	return u
}

// Ignore sets each column to itself in case of conflict.
// Using this option is equivalent to using:
//
//	client.LeafDecomposition.Create().
//	    OnConflict(sql.ResolveWithIgnore()).
//	    Exec(ctx)
func (u *LeafDecompositionUpsertOne) Ignore() *LeafDecompositionUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithIgnore())
	return u
}

// DoNothing configures the conflict_action to `DO NOTHING`.
// Supported only by SQLite and PostgreSQL.
func (u *LeafDecompositionUpsertOne) DoNothing() *LeafDecompositionUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.DoNothing())
	return u
}

// Update allows overriding fields `UPDATE` values. See the LeafDecompositionCreate.OnConflict
// documentation for more info.
func (u *LeafDecompositionUpsertOne) Update(set func(*LeafDecompositionUpsert)) *LeafDecompositionUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(update *sql.UpdateSet) {
		set(&LeafDecompositionUpsert{UpdateSet: update})
	}))
	return u
}

// SetUpdateTime sets the "update_time" field.
func (u *LeafDecompositionUpsertOne) SetUpdateTime(v time.Time) *LeafDecompositionUpsertOne {
	return u.Update(func(s *LeafDecompositionUpsert) {
		s.SetUpdateTime(v)
	})
}

// UpdateUpdateTime sets the "update_time" field to the value that was provided on create.
func (u *LeafDecompositionUpsertOne) UpdateUpdateTime() *LeafDecompositionUpsertOne {
	return u.Update(func(s *LeafDecompositionUpsert) {
		s.UpdateUpdateTime()
	})
}

// SetStatus sets the "status" field.
func (u *LeafDecompositionUpsertOne) SetStatus(v schematype.LeafDecompositionStatus) *LeafDecompositionUpsertOne {
	return u.Update(func(s *LeafDecompositionUpsert) {
		s.SetStatus(v)
	})
}

// UpdateStatus sets the "status" field to the value that was provided on create.
func (u *LeafDecompositionUpsertOne) UpdateStatus() *LeafDecompositionUpsertOne {
	return u.Update(func(s *LeafDecompositionUpsert) {
		s.UpdateStatus()
	})
}

// Exec executes the query.
func (u *LeafDecompositionUpsertOne) Exec(ctx context.Context) error {
	if len(u.create.conflict) == 0 {
		return errors.New("ent: missing options for LeafDecompositionCreate.OnConflict")
	}
	return u.create.Exec(ctx)
}

// ExecX is like Exec, but panics if an error occurs.
func (u *LeafDecompositionUpsertOne) ExecX(ctx context.Context) {
	if err := u.create.Exec(ctx); err != nil {
		panic(err)
	}
}

// Exec executes the UPSERT query and returns the inserted/updated ID.
func (u *LeafDecompositionUpsertOne) ID(ctx context.Context) (id uuid.UUID, err error) {
	if u.create.driver.Dialect() == dialect.MySQL {
		// In case of "ON CONFLICT", there is no way to get back non-numeric ID
		// fields from the database since MySQL does not support the RETURNING clause.
		return id, errors.New("ent: LeafDecompositionUpsertOne.ID is not supported by MySQL driver. Use LeafDecompositionUpsertOne.Exec instead")
	}
	node, err := u.create.Save(ctx)
	if err != nil {
		return id, err
	}
	return node.ID, nil
}

// IDX is like ID, but panics if an error occurs.
func (u *LeafDecompositionUpsertOne) IDX(ctx context.Context) uuid.UUID {
	id, err := u.ID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// LeafDecompositionCreateBulk is the builder for creating many LeafDecomposition entities in bulk.
type LeafDecompositionCreateBulk struct {
	config
	err      error
	builders []*LeafDecompositionCreate
	conflict []sql.ConflictOption
}

// Save creates the LeafDecomposition entities in the database.
func (ldcb *LeafDecompositionCreateBulk) Save(ctx context.Context) ([]*LeafDecomposition, error) {
	if ldcb.err != nil {
		return nil, ldcb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(ldcb.builders))
	nodes := make([]*LeafDecomposition, len(ldcb.builders))
	mutators := make([]Mutator, len(ldcb.builders))
	for i := range ldcb.builders {
		func(i int, root context.Context) {
			builder := ldcb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*LeafDecompositionMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, ldcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					spec.OnConflict = ldcb.conflict
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, ldcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, ldcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (ldcb *LeafDecompositionCreateBulk) SaveX(ctx context.Context) []*LeafDecomposition {
	v, err := ldcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (ldcb *LeafDecompositionCreateBulk) Exec(ctx context.Context) error {
	_, err := ldcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (ldcb *LeafDecompositionCreateBulk) ExecX(ctx context.Context) {
	if err := ldcb.Exec(ctx); err != nil {
		panic(err)
	}
}

// OnConflict allows configuring the `ON CONFLICT` / `ON DUPLICATE KEY` clause
// of the `INSERT` statement. For example:
//
//	client.LeafDecomposition.CreateBulk(builders...).
//		OnConflict(
//			// Update the row with the new values
//			// the was proposed for insertion.
//			sql.ResolveWithNewValues(),
//		).
//		// Override some of the fields with custom
//		// update values.
//		Update(func(u *ent.LeafDecompositionUpsert) {
//			SetCreateTime(v+v).
//		}).
//		Exec(ctx)
func (ldcb *LeafDecompositionCreateBulk) OnConflict(opts ...sql.ConflictOption) *LeafDecompositionUpsertBulk {
	ldcb.conflict = opts
	return &LeafDecompositionUpsertBulk{
		create: ldcb,
	}
}

// OnConflictColumns calls `OnConflict` and configures the columns
// as conflict target. Using this option is equivalent to using:
//
//	client.LeafDecomposition.Create().
//		OnConflict(sql.ConflictColumns(columns...)).
//		Exec(ctx)
func (ldcb *LeafDecompositionCreateBulk) OnConflictColumns(columns ...string) *LeafDecompositionUpsertBulk {
	ldcb.conflict = append(ldcb.conflict, sql.ConflictColumns(columns...))
	return &LeafDecompositionUpsertBulk{
		create: ldcb,
	}
}

// LeafDecompositionUpsertBulk is the builder for "upsert"-ing
// a bulk of LeafDecomposition nodes.
type LeafDecompositionUpsertBulk struct {
	create *LeafDecompositionCreateBulk
}

// UpdateNewValues updates the mutable fields using the new values that
// were set on create. Using this option is equivalent to using:
//
//	client.LeafDecomposition.Create().
//		OnConflict(
//			sql.ResolveWithNewValues(),
//			sql.ResolveWith(func(u *sql.UpdateSet) {
//				u.SetIgnore(leafdecomposition.FieldID)
//			}),
//		).
//		Exec(ctx)
func (u *LeafDecompositionUpsertBulk) UpdateNewValues() *LeafDecompositionUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithNewValues())
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(s *sql.UpdateSet) {
		for _, b := range u.create.builders {
			if _, exists := b.mutation.ID(); exists {
				s.SetIgnore(leafdecomposition.FieldID)
			}
			if _, exists := b.mutation.CreateTime(); exists {
				s.SetIgnore(leafdecomposition.FieldCreateTime)
			}
			if _, exists := b.mutation.DelegateSigningPubkey(); exists {
				s.SetIgnore(leafdecomposition.FieldDelegateSigningPubkey)
			}
		}
	}))
	return u
}

// Ignore sets each column to itself in case of conflict.
// Using this option is equivalent to using:
//
//	client.LeafDecomposition.Create().
//		OnConflict(sql.ResolveWithIgnore()).
//		Exec(ctx)
func (u *LeafDecompositionUpsertBulk) Ignore() *LeafDecompositionUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithIgnore())
	return u
}

// DoNothing configures the conflict_action to `DO NOTHING`.
// Supported only by SQLite and PostgreSQL.
func (u *LeafDecompositionUpsertBulk) DoNothing() *LeafDecompositionUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.DoNothing())
	return u
}

// Update allows overriding fields `UPDATE` values. See the LeafDecompositionCreateBulk.OnConflict
// documentation for more info.
func (u *LeafDecompositionUpsertBulk) Update(set func(*LeafDecompositionUpsert)) *LeafDecompositionUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(update *sql.UpdateSet) {
		set(&LeafDecompositionUpsert{UpdateSet: update})
	}))
	return u
}

// SetUpdateTime sets the "update_time" field.
func (u *LeafDecompositionUpsertBulk) SetUpdateTime(v time.Time) *LeafDecompositionUpsertBulk {
	return u.Update(func(s *LeafDecompositionUpsert) {
		s.SetUpdateTime(v)
	})
}

// UpdateUpdateTime sets the "update_time" field to the value that was provided on create.
func (u *LeafDecompositionUpsertBulk) UpdateUpdateTime() *LeafDecompositionUpsertBulk {
	return u.Update(func(s *LeafDecompositionUpsert) {
		s.UpdateUpdateTime()
	})
}

// SetStatus sets the "status" field.
func (u *LeafDecompositionUpsertBulk) SetStatus(v schematype.LeafDecompositionStatus) *LeafDecompositionUpsertBulk {
	return u.Update(func(s *LeafDecompositionUpsert) {
		s.SetStatus(v)
	})
}

// UpdateStatus sets the "status" field to the value that was provided on create.
func (u *LeafDecompositionUpsertBulk) UpdateStatus() *LeafDecompositionUpsertBulk {
	return u.Update(func(s *LeafDecompositionUpsert) {
		s.UpdateStatus()
	})
}

// Exec executes the query.
func (u *LeafDecompositionUpsertBulk) Exec(ctx context.Context) error {
	if u.create.err != nil {
		return u.create.err
	}
	for i, b := range u.create.builders {
		if len(b.conflict) != 0 {
			return fmt.Errorf("ent: OnConflict was set for builder %d. Set it on the LeafDecompositionCreateBulk instead", i)
		}
	}
	if len(u.create.conflict) == 0 {
		return errors.New("ent: missing options for LeafDecompositionCreateBulk.OnConflict")
	}
	return u.create.Exec(ctx)
}

// ExecX is like Exec, but panics if an error occurs.
func (u *LeafDecompositionUpsertBulk) ExecX(ctx context.Context) {
	if err := u.create.Exec(ctx); err != nil {
		panic(err)
	}
}
