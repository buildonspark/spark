// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/lightsparkdev/spark/so/ent/eventmessage"
	"github.com/lightsparkdev/spark/so/ent/predicate"
)

// EventMessageQuery is the builder for querying EventMessage entities.
type EventMessageQuery struct {
	config
	ctx        *QueryContext
	order      []eventmessage.OrderOption
	inters     []Interceptor
	predicates []predicate.EventMessage
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the EventMessageQuery builder.
func (emq *EventMessageQuery) Where(ps ...predicate.EventMessage) *EventMessageQuery {
	emq.predicates = append(emq.predicates, ps...)
	return emq
}

// Limit the number of records to be returned by this query.
func (emq *EventMessageQuery) Limit(limit int) *EventMessageQuery {
	emq.ctx.Limit = &limit
	return emq
}

// Offset to start from.
func (emq *EventMessageQuery) Offset(offset int) *EventMessageQuery {
	emq.ctx.Offset = &offset
	return emq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (emq *EventMessageQuery) Unique(unique bool) *EventMessageQuery {
	emq.ctx.Unique = &unique
	return emq
}

// Order specifies how the records should be ordered.
func (emq *EventMessageQuery) Order(o ...eventmessage.OrderOption) *EventMessageQuery {
	emq.order = append(emq.order, o...)
	return emq
}

// First returns the first EventMessage entity from the query.
// Returns a *NotFoundError when no EventMessage was found.
func (emq *EventMessageQuery) First(ctx context.Context) (*EventMessage, error) {
	nodes, err := emq.Limit(1).All(setContextOp(ctx, emq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{eventmessage.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (emq *EventMessageQuery) FirstX(ctx context.Context) *EventMessage {
	node, err := emq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first EventMessage ID from the query.
// Returns a *NotFoundError when no EventMessage ID was found.
func (emq *EventMessageQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = emq.Limit(1).IDs(setContextOp(ctx, emq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{eventmessage.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (emq *EventMessageQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := emq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single EventMessage entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one EventMessage entity is found.
// Returns a *NotFoundError when no EventMessage entities are found.
func (emq *EventMessageQuery) Only(ctx context.Context) (*EventMessage, error) {
	nodes, err := emq.Limit(2).All(setContextOp(ctx, emq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{eventmessage.Label}
	default:
		return nil, &NotSingularError{eventmessage.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (emq *EventMessageQuery) OnlyX(ctx context.Context) *EventMessage {
	node, err := emq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only EventMessage ID in the query.
// Returns a *NotSingularError when more than one EventMessage ID is found.
// Returns a *NotFoundError when no entities are found.
func (emq *EventMessageQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = emq.Limit(2).IDs(setContextOp(ctx, emq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{eventmessage.Label}
	default:
		err = &NotSingularError{eventmessage.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (emq *EventMessageQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := emq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of EventMessages.
func (emq *EventMessageQuery) All(ctx context.Context) ([]*EventMessage, error) {
	ctx = setContextOp(ctx, emq.ctx, ent.OpQueryAll)
	if err := emq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*EventMessage, *EventMessageQuery]()
	return withInterceptors[[]*EventMessage](ctx, emq, qr, emq.inters)
}

// AllX is like All, but panics if an error occurs.
func (emq *EventMessageQuery) AllX(ctx context.Context) []*EventMessage {
	nodes, err := emq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of EventMessage IDs.
func (emq *EventMessageQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if emq.ctx.Unique == nil && emq.path != nil {
		emq.Unique(true)
	}
	ctx = setContextOp(ctx, emq.ctx, ent.OpQueryIDs)
	if err = emq.Select(eventmessage.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (emq *EventMessageQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := emq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (emq *EventMessageQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, emq.ctx, ent.OpQueryCount)
	if err := emq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, emq, querierCount[*EventMessageQuery](), emq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (emq *EventMessageQuery) CountX(ctx context.Context) int {
	count, err := emq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (emq *EventMessageQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, emq.ctx, ent.OpQueryExist)
	switch _, err := emq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (emq *EventMessageQuery) ExistX(ctx context.Context) bool {
	exist, err := emq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the EventMessageQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (emq *EventMessageQuery) Clone() *EventMessageQuery {
	if emq == nil {
		return nil
	}
	return &EventMessageQuery{
		config:     emq.config,
		ctx:        emq.ctx.Clone(),
		order:      append([]eventmessage.OrderOption{}, emq.order...),
		inters:     append([]Interceptor{}, emq.inters...),
		predicates: append([]predicate.EventMessage{}, emq.predicates...),
		// clone intermediate query.
		sql:       emq.sql.Clone(),
		path:      emq.path,
		modifiers: append([]func(*sql.Selector){}, emq.modifiers...),
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.EventMessage.Query().
//		GroupBy(eventmessage.FieldCreateTime).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (emq *EventMessageQuery) GroupBy(field string, fields ...string) *EventMessageGroupBy {
	emq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &EventMessageGroupBy{build: emq}
	grbuild.flds = &emq.ctx.Fields
	grbuild.label = eventmessage.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//	}
//
//	client.EventMessage.Query().
//		Select(eventmessage.FieldCreateTime).
//		Scan(ctx, &v)
func (emq *EventMessageQuery) Select(fields ...string) *EventMessageSelect {
	emq.ctx.Fields = append(emq.ctx.Fields, fields...)
	sbuild := &EventMessageSelect{EventMessageQuery: emq}
	sbuild.label = eventmessage.Label
	sbuild.flds, sbuild.scan = &emq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a EventMessageSelect configured with the given aggregations.
func (emq *EventMessageQuery) Aggregate(fns ...AggregateFunc) *EventMessageSelect {
	return emq.Select().Aggregate(fns...)
}

func (emq *EventMessageQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range emq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, emq); err != nil {
				return err
			}
		}
	}
	for _, f := range emq.ctx.Fields {
		if !eventmessage.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if emq.path != nil {
		prev, err := emq.path(ctx)
		if err != nil {
			return err
		}
		emq.sql = prev
	}
	return nil
}

func (emq *EventMessageQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*EventMessage, error) {
	var (
		nodes = []*EventMessage{}
		_spec = emq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*EventMessage).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &EventMessage{config: emq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(emq.modifiers) > 0 {
		_spec.Modifiers = emq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, emq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (emq *EventMessageQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := emq.querySpec()
	if len(emq.modifiers) > 0 {
		_spec.Modifiers = emq.modifiers
	}
	_spec.Node.Columns = emq.ctx.Fields
	if len(emq.ctx.Fields) > 0 {
		_spec.Unique = emq.ctx.Unique != nil && *emq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, emq.driver, _spec)
}

func (emq *EventMessageQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(eventmessage.Table, eventmessage.Columns, sqlgraph.NewFieldSpec(eventmessage.FieldID, field.TypeUUID))
	_spec.From = emq.sql
	if unique := emq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if emq.path != nil {
		_spec.Unique = true
	}
	if fields := emq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, eventmessage.FieldID)
		for i := range fields {
			if fields[i] != eventmessage.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := emq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := emq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := emq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := emq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (emq *EventMessageQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(emq.driver.Dialect())
	t1 := builder.Table(eventmessage.Table)
	columns := emq.ctx.Fields
	if len(columns) == 0 {
		columns = eventmessage.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if emq.sql != nil {
		selector = emq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if emq.ctx.Unique != nil && *emq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range emq.modifiers {
		m(selector)
	}
	for _, p := range emq.predicates {
		p(selector)
	}
	for _, p := range emq.order {
		p(selector)
	}
	if offset := emq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := emq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (emq *EventMessageQuery) ForUpdate(opts ...sql.LockOption) *EventMessageQuery {
	if emq.driver.Dialect() == dialect.Postgres {
		emq.Unique(false)
	}
	emq.modifiers = append(emq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return emq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (emq *EventMessageQuery) ForShare(opts ...sql.LockOption) *EventMessageQuery {
	if emq.driver.Dialect() == dialect.Postgres {
		emq.Unique(false)
	}
	emq.modifiers = append(emq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return emq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (emq *EventMessageQuery) Modify(modifiers ...func(s *sql.Selector)) *EventMessageSelect {
	emq.modifiers = append(emq.modifiers, modifiers...)
	return emq.Select()
}

// EventMessageGroupBy is the group-by builder for EventMessage entities.
type EventMessageGroupBy struct {
	selector
	build *EventMessageQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (emgb *EventMessageGroupBy) Aggregate(fns ...AggregateFunc) *EventMessageGroupBy {
	emgb.fns = append(emgb.fns, fns...)
	return emgb
}

// Scan applies the selector query and scans the result into the given value.
func (emgb *EventMessageGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, emgb.build.ctx, ent.OpQueryGroupBy)
	if err := emgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EventMessageQuery, *EventMessageGroupBy](ctx, emgb.build, emgb, emgb.build.inters, v)
}

func (emgb *EventMessageGroupBy) sqlScan(ctx context.Context, root *EventMessageQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(emgb.fns))
	for _, fn := range emgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*emgb.flds)+len(emgb.fns))
		for _, f := range *emgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*emgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := emgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// EventMessageSelect is the builder for selecting fields of EventMessage entities.
type EventMessageSelect struct {
	*EventMessageQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ems *EventMessageSelect) Aggregate(fns ...AggregateFunc) *EventMessageSelect {
	ems.fns = append(ems.fns, fns...)
	return ems
}

// Scan applies the selector query and scans the result into the given value.
func (ems *EventMessageSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ems.ctx, ent.OpQuerySelect)
	if err := ems.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EventMessageQuery, *EventMessageSelect](ctx, ems.EventMessageQuery, ems, ems.inters, v)
}

func (ems *EventMessageSelect) sqlScan(ctx context.Context, root *EventMessageQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ems.fns))
	for _, fn := range ems.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ems.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ems.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ems *EventMessageSelect) Modify(modifiers ...func(s *sql.Selector)) *EventMessageSelect {
	ems.modifiers = append(ems.modifiers, modifiers...)
	return ems
}
