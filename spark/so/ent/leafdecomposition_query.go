// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/lightsparkdev/spark/so/ent/delegationgrant"
	"github.com/lightsparkdev/spark/so/ent/leafdecomposition"
	"github.com/lightsparkdev/spark/so/ent/predicate"
	"github.com/lightsparkdev/spark/so/ent/signingkeyshare"
	"github.com/lightsparkdev/spark/so/ent/treenode"
)

// LeafDecompositionQuery is the builder for querying LeafDecomposition entities.
type LeafDecompositionQuery struct {
	config
	ctx                 *QueryContext
	order               []leafdecomposition.OrderOption
	inters              []Interceptor
	predicates          []predicate.LeafDecomposition
	withTreeNode        *TreeNodeQuery
	withSigningKeyshare *SigningKeyshareQuery
	withDelegationGrant *DelegationGrantQuery
	withFKs             bool
	modifiers           []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the LeafDecompositionQuery builder.
func (ldq *LeafDecompositionQuery) Where(ps ...predicate.LeafDecomposition) *LeafDecompositionQuery {
	ldq.predicates = append(ldq.predicates, ps...)
	return ldq
}

// Limit the number of records to be returned by this query.
func (ldq *LeafDecompositionQuery) Limit(limit int) *LeafDecompositionQuery {
	ldq.ctx.Limit = &limit
	return ldq
}

// Offset to start from.
func (ldq *LeafDecompositionQuery) Offset(offset int) *LeafDecompositionQuery {
	ldq.ctx.Offset = &offset
	return ldq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ldq *LeafDecompositionQuery) Unique(unique bool) *LeafDecompositionQuery {
	ldq.ctx.Unique = &unique
	return ldq
}

// Order specifies how the records should be ordered.
func (ldq *LeafDecompositionQuery) Order(o ...leafdecomposition.OrderOption) *LeafDecompositionQuery {
	ldq.order = append(ldq.order, o...)
	return ldq
}

// QueryTreeNode chains the current query on the "tree_node" edge.
func (ldq *LeafDecompositionQuery) QueryTreeNode() *TreeNodeQuery {
	query := (&TreeNodeClient{config: ldq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ldq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ldq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(leafdecomposition.Table, leafdecomposition.FieldID, selector),
			sqlgraph.To(treenode.Table, treenode.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, leafdecomposition.TreeNodeTable, leafdecomposition.TreeNodeColumn),
		)
		fromU = sqlgraph.SetNeighbors(ldq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QuerySigningKeyshare chains the current query on the "signing_keyshare" edge.
func (ldq *LeafDecompositionQuery) QuerySigningKeyshare() *SigningKeyshareQuery {
	query := (&SigningKeyshareClient{config: ldq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ldq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ldq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(leafdecomposition.Table, leafdecomposition.FieldID, selector),
			sqlgraph.To(signingkeyshare.Table, signingkeyshare.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, leafdecomposition.SigningKeyshareTable, leafdecomposition.SigningKeyshareColumn),
		)
		fromU = sqlgraph.SetNeighbors(ldq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryDelegationGrant chains the current query on the "delegation_grant" edge.
func (ldq *LeafDecompositionQuery) QueryDelegationGrant() *DelegationGrantQuery {
	query := (&DelegationGrantClient{config: ldq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ldq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ldq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(leafdecomposition.Table, leafdecomposition.FieldID, selector),
			sqlgraph.To(delegationgrant.Table, delegationgrant.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, leafdecomposition.DelegationGrantTable, leafdecomposition.DelegationGrantColumn),
		)
		fromU = sqlgraph.SetNeighbors(ldq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first LeafDecomposition entity from the query.
// Returns a *NotFoundError when no LeafDecomposition was found.
func (ldq *LeafDecompositionQuery) First(ctx context.Context) (*LeafDecomposition, error) {
	nodes, err := ldq.Limit(1).All(setContextOp(ctx, ldq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{leafdecomposition.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ldq *LeafDecompositionQuery) FirstX(ctx context.Context) *LeafDecomposition {
	node, err := ldq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first LeafDecomposition ID from the query.
// Returns a *NotFoundError when no LeafDecomposition ID was found.
func (ldq *LeafDecompositionQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = ldq.Limit(1).IDs(setContextOp(ctx, ldq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{leafdecomposition.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ldq *LeafDecompositionQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := ldq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single LeafDecomposition entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one LeafDecomposition entity is found.
// Returns a *NotFoundError when no LeafDecomposition entities are found.
func (ldq *LeafDecompositionQuery) Only(ctx context.Context) (*LeafDecomposition, error) {
	nodes, err := ldq.Limit(2).All(setContextOp(ctx, ldq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{leafdecomposition.Label}
	default:
		return nil, &NotSingularError{leafdecomposition.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ldq *LeafDecompositionQuery) OnlyX(ctx context.Context) *LeafDecomposition {
	node, err := ldq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only LeafDecomposition ID in the query.
// Returns a *NotSingularError when more than one LeafDecomposition ID is found.
// Returns a *NotFoundError when no entities are found.
func (ldq *LeafDecompositionQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = ldq.Limit(2).IDs(setContextOp(ctx, ldq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{leafdecomposition.Label}
	default:
		err = &NotSingularError{leafdecomposition.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ldq *LeafDecompositionQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := ldq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of LeafDecompositions.
func (ldq *LeafDecompositionQuery) All(ctx context.Context) ([]*LeafDecomposition, error) {
	ctx = setContextOp(ctx, ldq.ctx, ent.OpQueryAll)
	if err := ldq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*LeafDecomposition, *LeafDecompositionQuery]()
	return withInterceptors[[]*LeafDecomposition](ctx, ldq, qr, ldq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ldq *LeafDecompositionQuery) AllX(ctx context.Context) []*LeafDecomposition {
	nodes, err := ldq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of LeafDecomposition IDs.
func (ldq *LeafDecompositionQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if ldq.ctx.Unique == nil && ldq.path != nil {
		ldq.Unique(true)
	}
	ctx = setContextOp(ctx, ldq.ctx, ent.OpQueryIDs)
	if err = ldq.Select(leafdecomposition.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ldq *LeafDecompositionQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := ldq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ldq *LeafDecompositionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ldq.ctx, ent.OpQueryCount)
	if err := ldq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ldq, querierCount[*LeafDecompositionQuery](), ldq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ldq *LeafDecompositionQuery) CountX(ctx context.Context) int {
	count, err := ldq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ldq *LeafDecompositionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ldq.ctx, ent.OpQueryExist)
	switch _, err := ldq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ldq *LeafDecompositionQuery) ExistX(ctx context.Context) bool {
	exist, err := ldq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the LeafDecompositionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ldq *LeafDecompositionQuery) Clone() *LeafDecompositionQuery {
	if ldq == nil {
		return nil
	}
	return &LeafDecompositionQuery{
		config:              ldq.config,
		ctx:                 ldq.ctx.Clone(),
		order:               append([]leafdecomposition.OrderOption{}, ldq.order...),
		inters:              append([]Interceptor{}, ldq.inters...),
		predicates:          append([]predicate.LeafDecomposition{}, ldq.predicates...),
		withTreeNode:        ldq.withTreeNode.Clone(),
		withSigningKeyshare: ldq.withSigningKeyshare.Clone(),
		withDelegationGrant: ldq.withDelegationGrant.Clone(),
		// clone intermediate query.
		sql:       ldq.sql.Clone(),
		path:      ldq.path,
		modifiers: append([]func(*sql.Selector){}, ldq.modifiers...),
	}
}

// WithTreeNode tells the query-builder to eager-load the nodes that are connected to
// the "tree_node" edge. The optional arguments are used to configure the query builder of the edge.
func (ldq *LeafDecompositionQuery) WithTreeNode(opts ...func(*TreeNodeQuery)) *LeafDecompositionQuery {
	query := (&TreeNodeClient{config: ldq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ldq.withTreeNode = query
	return ldq
}

// WithSigningKeyshare tells the query-builder to eager-load the nodes that are connected to
// the "signing_keyshare" edge. The optional arguments are used to configure the query builder of the edge.
func (ldq *LeafDecompositionQuery) WithSigningKeyshare(opts ...func(*SigningKeyshareQuery)) *LeafDecompositionQuery {
	query := (&SigningKeyshareClient{config: ldq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ldq.withSigningKeyshare = query
	return ldq
}

// WithDelegationGrant tells the query-builder to eager-load the nodes that are connected to
// the "delegation_grant" edge. The optional arguments are used to configure the query builder of the edge.
func (ldq *LeafDecompositionQuery) WithDelegationGrant(opts ...func(*DelegationGrantQuery)) *LeafDecompositionQuery {
	query := (&DelegationGrantClient{config: ldq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ldq.withDelegationGrant = query
	return ldq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.LeafDecomposition.Query().
//		GroupBy(leafdecomposition.FieldCreateTime).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ldq *LeafDecompositionQuery) GroupBy(field string, fields ...string) *LeafDecompositionGroupBy {
	ldq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &LeafDecompositionGroupBy{build: ldq}
	grbuild.flds = &ldq.ctx.Fields
	grbuild.label = leafdecomposition.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//	}
//
//	client.LeafDecomposition.Query().
//		Select(leafdecomposition.FieldCreateTime).
//		Scan(ctx, &v)
func (ldq *LeafDecompositionQuery) Select(fields ...string) *LeafDecompositionSelect {
	ldq.ctx.Fields = append(ldq.ctx.Fields, fields...)
	sbuild := &LeafDecompositionSelect{LeafDecompositionQuery: ldq}
	sbuild.label = leafdecomposition.Label
	sbuild.flds, sbuild.scan = &ldq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a LeafDecompositionSelect configured with the given aggregations.
func (ldq *LeafDecompositionQuery) Aggregate(fns ...AggregateFunc) *LeafDecompositionSelect {
	return ldq.Select().Aggregate(fns...)
}

func (ldq *LeafDecompositionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ldq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ldq); err != nil {
				return err
			}
		}
	}
	for _, f := range ldq.ctx.Fields {
		if !leafdecomposition.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ldq.path != nil {
		prev, err := ldq.path(ctx)
		if err != nil {
			return err
		}
		ldq.sql = prev
	}
	return nil
}

func (ldq *LeafDecompositionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*LeafDecomposition, error) {
	var (
		nodes       = []*LeafDecomposition{}
		withFKs     = ldq.withFKs
		_spec       = ldq.querySpec()
		loadedTypes = [3]bool{
			ldq.withTreeNode != nil,
			ldq.withSigningKeyshare != nil,
			ldq.withDelegationGrant != nil,
		}
	)
	if ldq.withTreeNode != nil || ldq.withSigningKeyshare != nil || ldq.withDelegationGrant != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, leafdecomposition.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*LeafDecomposition).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &LeafDecomposition{config: ldq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(ldq.modifiers) > 0 {
		_spec.Modifiers = ldq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ldq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := ldq.withTreeNode; query != nil {
		if err := ldq.loadTreeNode(ctx, query, nodes, nil,
			func(n *LeafDecomposition, e *TreeNode) { n.Edges.TreeNode = e }); err != nil {
			return nil, err
		}
	}
	if query := ldq.withSigningKeyshare; query != nil {
		if err := ldq.loadSigningKeyshare(ctx, query, nodes, nil,
			func(n *LeafDecomposition, e *SigningKeyshare) { n.Edges.SigningKeyshare = e }); err != nil {
			return nil, err
		}
	}
	if query := ldq.withDelegationGrant; query != nil {
		if err := ldq.loadDelegationGrant(ctx, query, nodes, nil,
			func(n *LeafDecomposition, e *DelegationGrant) { n.Edges.DelegationGrant = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (ldq *LeafDecompositionQuery) loadTreeNode(ctx context.Context, query *TreeNodeQuery, nodes []*LeafDecomposition, init func(*LeafDecomposition), assign func(*LeafDecomposition, *TreeNode)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*LeafDecomposition)
	for i := range nodes {
		if nodes[i].leaf_decomposition_tree_node == nil {
			continue
		}
		fk := *nodes[i].leaf_decomposition_tree_node
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(treenode.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "leaf_decomposition_tree_node" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (ldq *LeafDecompositionQuery) loadSigningKeyshare(ctx context.Context, query *SigningKeyshareQuery, nodes []*LeafDecomposition, init func(*LeafDecomposition), assign func(*LeafDecomposition, *SigningKeyshare)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*LeafDecomposition)
	for i := range nodes {
		if nodes[i].leaf_decomposition_signing_keyshare == nil {
			continue
		}
		fk := *nodes[i].leaf_decomposition_signing_keyshare
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(signingkeyshare.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "leaf_decomposition_signing_keyshare" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (ldq *LeafDecompositionQuery) loadDelegationGrant(ctx context.Context, query *DelegationGrantQuery, nodes []*LeafDecomposition, init func(*LeafDecomposition), assign func(*LeafDecomposition, *DelegationGrant)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*LeafDecomposition)
	for i := range nodes {
		if nodes[i].delegation_grant_leaf_decompositions == nil {
			continue
		}
		fk := *nodes[i].delegation_grant_leaf_decompositions
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(delegationgrant.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "delegation_grant_leaf_decompositions" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (ldq *LeafDecompositionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ldq.querySpec()
	if len(ldq.modifiers) > 0 {
		_spec.Modifiers = ldq.modifiers
	}
	_spec.Node.Columns = ldq.ctx.Fields
	if len(ldq.ctx.Fields) > 0 {
		_spec.Unique = ldq.ctx.Unique != nil && *ldq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ldq.driver, _spec)
}

func (ldq *LeafDecompositionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(leafdecomposition.Table, leafdecomposition.Columns, sqlgraph.NewFieldSpec(leafdecomposition.FieldID, field.TypeUUID))
	_spec.From = ldq.sql
	if unique := ldq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ldq.path != nil {
		_spec.Unique = true
	}
	if fields := ldq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, leafdecomposition.FieldID)
		for i := range fields {
			if fields[i] != leafdecomposition.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ldq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ldq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ldq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ldq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ldq *LeafDecompositionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ldq.driver.Dialect())
	t1 := builder.Table(leafdecomposition.Table)
	columns := ldq.ctx.Fields
	if len(columns) == 0 {
		columns = leafdecomposition.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ldq.sql != nil {
		selector = ldq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ldq.ctx.Unique != nil && *ldq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range ldq.modifiers {
		m(selector)
	}
	for _, p := range ldq.predicates {
		p(selector)
	}
	for _, p := range ldq.order {
		p(selector)
	}
	if offset := ldq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ldq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (ldq *LeafDecompositionQuery) ForUpdate(opts ...sql.LockOption) *LeafDecompositionQuery {
	if ldq.driver.Dialect() == dialect.Postgres {
		ldq.Unique(false)
	}
	ldq.modifiers = append(ldq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return ldq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (ldq *LeafDecompositionQuery) ForShare(opts ...sql.LockOption) *LeafDecompositionQuery {
	if ldq.driver.Dialect() == dialect.Postgres {
		ldq.Unique(false)
	}
	ldq.modifiers = append(ldq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return ldq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ldq *LeafDecompositionQuery) Modify(modifiers ...func(s *sql.Selector)) *LeafDecompositionSelect {
	ldq.modifiers = append(ldq.modifiers, modifiers...)
	return ldq.Select()
}

// LeafDecompositionGroupBy is the group-by builder for LeafDecomposition entities.
type LeafDecompositionGroupBy struct {
	selector
	build *LeafDecompositionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ldgb *LeafDecompositionGroupBy) Aggregate(fns ...AggregateFunc) *LeafDecompositionGroupBy {
	ldgb.fns = append(ldgb.fns, fns...)
	return ldgb
}

// Scan applies the selector query and scans the result into the given value.
func (ldgb *LeafDecompositionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ldgb.build.ctx, ent.OpQueryGroupBy)
	if err := ldgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LeafDecompositionQuery, *LeafDecompositionGroupBy](ctx, ldgb.build, ldgb, ldgb.build.inters, v)
}

func (ldgb *LeafDecompositionGroupBy) sqlScan(ctx context.Context, root *LeafDecompositionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ldgb.fns))
	for _, fn := range ldgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ldgb.flds)+len(ldgb.fns))
		for _, f := range *ldgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ldgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ldgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// LeafDecompositionSelect is the builder for selecting fields of LeafDecomposition entities.
type LeafDecompositionSelect struct {
	*LeafDecompositionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (lds *LeafDecompositionSelect) Aggregate(fns ...AggregateFunc) *LeafDecompositionSelect {
	lds.fns = append(lds.fns, fns...)
	return lds
}

// Scan applies the selector query and scans the result into the given value.
func (lds *LeafDecompositionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, lds.ctx, ent.OpQuerySelect)
	if err := lds.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LeafDecompositionQuery, *LeafDecompositionSelect](ctx, lds.LeafDecompositionQuery, lds, lds.inters, v)
}

func (lds *LeafDecompositionSelect) sqlScan(ctx context.Context, root *LeafDecompositionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(lds.fns))
	for _, fn := range lds.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*lds.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := lds.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (lds *LeafDecompositionSelect) Modify(modifiers ...func(s *sql.Selector)) *LeafDecompositionSelect {
	lds.modifiers = append(lds.modifiers, modifiers...)
	return lds
}
